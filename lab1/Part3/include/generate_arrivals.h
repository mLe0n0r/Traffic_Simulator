#ifndef GENERATE_EVENTS_H
#define GENERATE_EVENTS_H

#include <stdio.h>
#include <stdlib.h>
#include "linked-list.h"

// Function prototypes
list* generate_events(double lambda, double event_time, list *list);

#endif
